// import jwt_decode from 'jwt-decode';

// // 解析 token，获取用户权限
// export function parseToken(token) {
//     try {
//         const decoded = jwt_decode(token);
//         const { identity } = decoded; // 假设 token 中包含 userId、username 和 role

//         return { identity };
//     } catch (error) {
//         console.error('Failed to parse token:', error);
//         return null;
//     }
// }
